spring.application.name=shoppers-backend

# Supabase PostgreSQL (from env)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.tjyvvreioqlpltyjukyc.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Harsha9000513338}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL10Dialect

# Security
shoppers.app.jwtSecret=${SHOOPERS_APP_JWT_SECRET:shoppersSecretKeyForJwtTokenGenerationPurposeOnlyUseStrongerKeyInProduction1234567890}
shoppers.app.jwtExpirationMs=${SHOOPERS_APP_JWT_EXPIRATION_MS:86400000}

# Server
server.port=${SERVER_PORT:8080}

# OAuth2 (Placeholder values)
spring.security.oauth2.client.registration.google.client-id=${OAUTH_GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${OAUTH_GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/google

# Logging
logging.level.org.springframework.security=DEBUG
